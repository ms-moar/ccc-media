# но все равно есть ощущение что это из за инфраструктуры не д

**Автор**: @tommypoltev
**Дата**: 26.03.2026 16:11
**Топик**: OpenClaw

---

но все равно есть ощущение что это из за инфраструктуры не доконца настроенной Честный ответ: никакое обещание не поможет. Я — LLM, и в длинной сессии начинаю оптимизировать на скорость, "забывая" правила. Нужен **системный блок**, а не самодисциплина.

**3 конкретных меры:**

**1. Exec policy — запретить мне редактировать код на VPS**
Добавить в exec-approvals правило: `ssh ... sed/python/cat >` на проектные файлы → требует approval. Читать логи, проверять статус — можно. Менять файлы — блок.

**2. Шаблон в **`AGENTS.md`** — быстрый dev agent spawn**
Чтобы "через dev agent" было не сложнее чем "самому". Готовый шаблон копипаста:

```codex exec --full-auto "ТЗ" в workdir репо
```
Если spawn dev agent занимает 10 секунд — нет соблазна делать самому.

**3. Триггерное слово в **`SOUL.md`
Добавить явную проверку: "Если я собираюсь вызвать `sed -i`, `python3 -c` с записью в файл проекта, или `scp` на VPS — СТОП. Это dev agent задача."
